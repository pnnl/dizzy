name: Release

# Trunk-based: a release is just a version tag pushed to main.
#   git tag v0.2.0 && git push origin v0.2.0
#
# This builds the sdist + wheel and attaches them to a GitHub Release.
#
# DIZZY is NOT published to a package index, by decision — the GitHub Release
# and the git repository are the distribution. Dependents name the source
# explicitly (`dizzy[gen] @ git+https://github.com/PNNL/dizzy`), and generated
# workspaces do the same via `dizzy generate wiring --dizzy-source`. Note that
# the name `dizzy` on PyPI belongs to an unrelated project, so a bare
# requirement resolves to the wrong package.
on:
  push:
    tags:
      - "v*"

jobs:
  release:
    name: build + GitHub Release
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: astral-sh/setup-uv@v5
      - name: Verify tag matches package version
        run: |
          uv sync
          PKG="$(uv run python -c 'import dizzy; print(dizzy.__version__)')"
          TAG="${GITHUB_REF_NAME#v}"
          echo "tag=$TAG package=$PKG"
          test "$PKG" = "$TAG" || { echo "::error::tag $TAG != package version $PKG"; exit 1; }
      - run: rm -rf dist && uv build
      - name: Create release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh release create "$GITHUB_REF_NAME" dist/* \
            --title "$GITHUB_REF_NAME" \
            --notes "See [CHANGELOG.md](https://github.com/PNNL/dizzy/blob/main/CHANGELOG.md)."
